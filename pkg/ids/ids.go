package ids

// New generates a new unique identifier string.
// Business modules will replace this with a concrete strategy (UUID, etc.).
func New() string {
	// Placeholder for skeleton; implementation will be added with persistence layer.
	return ""
}
